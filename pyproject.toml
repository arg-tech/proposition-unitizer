[project]
name = "proposition-unitizer"
version = "0.1.0"
description = "Proposition Unitizer microservice"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "flask==3.1.3",
    "gunicorn==25.1.0",
    "prometheus-flask-exporter==0.23.2",
    "xaif-eval==0.0.9",
]
